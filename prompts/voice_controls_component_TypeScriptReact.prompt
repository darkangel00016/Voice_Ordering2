You are responsible for: Client component providing push-to-talk and playback controls, handling microphone permissions and speech synthesis, and emitting transcript events to the parent. This module exists because: Voice interaction controls are needed to start/stop listening and play assistant responses.

Requirements
1. Accept props: onTranscript: (text: string) => void, isListening: boolean.
2. Emit events: transcript, listeningChange.
3. Provide clear user feedback states and preserve accessibility.

Dependencies
shared_types_TypeScript.prompt
  <include>examples/shared_types_example.ts</include>

Instructions
- Keep the component focused on presentation + events; state is owned by the parent unless required.
- Gracefully handle missing browser capabilities (speech, mic, etc.).

Deliverable
- components/VoiceControls.tsx
